# Overseer
An AI agent for Overture maps. This is my project A implementation prototype

# Must include in root directory
Metrics folder unzipped and a python venv. Shouldn't be pushed through Git, hence the .gitignore file
